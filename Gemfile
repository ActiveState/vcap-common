source (ENV['RUBYGEMS_MIRROR'] or :rubygems)

gemspec

# EM fork for 1.9.3/2.0.0 safe blocking regions, EM.live_bindings, various fixes
gem 'eventmachine', '~> 1.0.0',
  :git => 'https://github.com/ttilley/eventmachine.git',
  :branch => 'as'

group :spec do
  gem 'rspec'
end

